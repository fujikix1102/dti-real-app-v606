# Backend stub runtime V1

Local diagnostic runtime wrapper over a locked synthetic schema-only cache identity.

Boundaries:

- no API server
- no network call
- no source download
- no raw DESI ingest
- no cache build
- no chi2 computation
- no likelihood
- no MCMC
- no posterior inference
- no physical claim
- no manuscript update

This directory is local implementation material only until a separate review and git-push request passes.
